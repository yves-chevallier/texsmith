# Code

``` py title="bubble_sort.py" linenums="1" hl_lines="2-3"
def bubble(xs):
    for i in xs:
        pass
```

Listing: Bubble sort. {#lst:bubble}

```text
┌──┐
│hi│
└──┘
```

Inline `#!py a-b.c`, plain `x-y`, and `#!c z|w`.
