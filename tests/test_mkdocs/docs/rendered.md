---
press:
  reader: html
---

# Rendered

This page is read from its rendered HTML, not from its Markdown: the PDF sees
what Material produced, so Material's own syntax reaches the book too —
++ctrl+x++ and ==a highlight== included.
