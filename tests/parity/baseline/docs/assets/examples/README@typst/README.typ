#set document(
  title: "Generated Examples",
)
#set page(
  paper: "a4",
  margin: 2.5cm,
  numbering: none,
  footer: context {
    if counter(page).final().first() > 1 {
      align(center)[#counter(page).get().first()]
    }
  },
)
#set text(font: "New Computer Modern", size: 11pt, lang: "en")
#set par(justify: true)
#show heading: set block(above: 1.8em, below: 1.0em)
#set heading(numbering: "1.1")

#align(center)[
  #text(size: 1.8em, weight: "bold")[Generated Examples]
]
#v(1.5em)

This folder contains generated examples. Run the following command to regenerate them before running the documentation build:

```bash
uv run ./scripts/generate_examples.py
```
