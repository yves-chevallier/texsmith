\documentclass[a4paper,twoside]{article}

\usepackage{morewrites}

\usepackage[svgnames,dvipsnames,x11names]{xcolor}
\usepackage{graphicx}
\usepackage[english]{babel}
\usepackage[colorlinks=true, linkcolor=NavyBlue, urlcolor=NavyBlue, citecolor=NavyBlue]{hyperref}
\usepackage[a4paper]{geometry}
\usepackage{ts-fonts}
\usepackage{amsmath}
\usepackage{float}
\usepackage{seqsplit}
\usepackage{ts-callouts}
\usepackage{ts-code}

\usepackage{lastpage}
\usepackage{refcount}
\makeatletter
\def\ps@plain{
  \let\@oddhead\@empty
  \let\@evenhead\@empty
  \def\@oddfoot{
    \hfil
    \ifnum\getpagerefnumber{LastPage}>1\relax
      \thepage
    \fi
    \hfil}
  \let\@evenfoot\@oddfoot
}
\makeatother

\title{Cooking Recipes}
\author{}\date{}

\begin{document}

\maketitle

\thispagestyle{plain}
TeXSmith isn't just for papers and slides -- it can plate up gorgeous recipes straight from structured data. Here's a French walnut cake expressed as YAML, pushed through a custom \texttt{recipe} template. Click the card to grab the PDF.

\begin{figure}[H]
    \centering

    \ifdefined\adjustbox

        \adjustbox{max width=\textwidth}{
            \includegraphics[width=0.6\linewidth]{snippet-<HASH>.pdf}
        }

    \else

        \includegraphics[width=0.6\linewidth]{snippet-<HASH>.pdf}

    \fi

\end{figure}
The authoring surface is pure data: \textbf{YAML} fields flow into a \LaTeX{} template, no Markdown gymnastics required. Swap the YAML for a DB/API payload and you've got a pipeline-ready recipe generator for your site or app.

\textbf{cake.yml}\par

\begin{code}{yaml}{}{}
\begin{Verbatim}[commandchars=\\\{\},breaklines, breakanywhere, commandchars=\\\{\}]

\end{Verbatim}
\end{code}
\textbf{manifest.toml}\par

\begin{code}{toml}{}{}
\begin{Verbatim}[commandchars=\\\{\},breaklines, breakanywhere, commandchars=\\\{\}]

\end{Verbatim}
\end{code}
\textbf{template.tex}\par

\begin{code}{tex}{}{}
\begin{Verbatim}[commandchars=\\\{\},breaklines, breakanywhere, commandchars=\\\{\}]

\end{Verbatim}
\end{code}
\begin{callout}[callout note]{Note}
The recipe layout used in this template was inspired by the Cours de cuisine created by M.-C. Bolle back in 1978. She produced an exceptional cookbook for the Department of Public Instruction of the Canton of Geneva, Switzerland.

I've never seen recipes presented quite this way anywhere else -- it's a uniquely clever system -- but I find it incredibly practical and efficient.
\end{callout}

\end{document}
