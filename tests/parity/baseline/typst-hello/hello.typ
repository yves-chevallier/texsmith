#set page(margin: 2.5cm)
#set text(font: "New Computer Modern", size: 11pt)
#set par(justify: true)

#align(center)[#text(size: 1.6em, weight: "bold")[Hello Typst]]
#v(1em)

= Hello Typst

This is the *smallest* possible TeXSmith document rendered through the
_Typst_ backend. It exercises a paragraph with `inline code`, a
#link("https://typst.app")[link], and a horizontal rule.

#line(length: 100%)

That is all.
