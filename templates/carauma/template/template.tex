\documentclass\BLOCK{ if class_options and class_options.strip() }[\VAR{class_options}]\BLOCK{ endif }{book}

\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX}
\setmainfont{EB Garamond}
\setsansfont{Latin Modern Sans}
\setmonofont{DejaVu Sans Mono}

\newcommand{\authorname}{\VAR{author|default('', true)|escape_latex}}
\newcommand{\translatorname}{\VAR{translator|default('', true)|escape_latex}}
\newcommand{\booktitle}{\VAR{title|default('', true)|escape_latex}}
\newcommand{\subtitle}{\VAR{subtitle|default('', true)|escape_latex}}
\newcommand{\publisher}{\VAR{publisher|default('', true)|escape_latex}}
\newcommand{\editionyear}{\VAR{edition_year|default('', true)|escape_latex}}
\newcommand{\isbn}{\VAR{isbn|default('', true)|escape_latex}}

\newcommand{\caraumaedition}{\VAR{edition_statement|default('', true)|escape_latex}}
\newcommand{\caraumalicense}{\VAR{license_text|default('', true)|escape_latex}}
\newcommand{\caraumacopyrightyear}{\VAR{copyright_year|default('', true)|escape_latex}}
\newcommand{\caraumacopyholder}{\VAR{copyright_holder|default('', true)|escape_latex}}
\newcommand{\caraumaprefaceheading}{\VAR{preface_heading|default('Prefácio', true)|escape_latex}}
\newcommand{\caraumabackcover}{\VAR{back_cover_text|default('', true)|escape_latex}}

\newcommand{\caraumabibliographystyle}{\VAR{bibliography_style|default('', true)|escape_latex}}
\newcommand{\caraumabibliography}{\VAR{bibliography|default('', true)|escape_latex}}

\BLOCK{ if language }
\newcommand{\caraumalanguage}{\VAR{language|escape_latex}}
\BLOCK{ else }
\newcommand{\caraumalanguage}{brazilian}
\BLOCK{ endif }

\usepackage[\caraumalanguage]{babel}
\BLOCK{ if contents_title and contents_title.strip() }
\AtBeginDocument{%
    \expandafter\addto\csname captions\caraumalanguage\endcsname{%
        \renewcommand{\contentsname}{\VAR{contents_title|escape_latex}}%
    }%
}
\BLOCK{ endif }

\usepackage{carauma-options}

\setlength{\parskip}{0pt}
\setlength{\parindent}{1em}

\begin{document}

\frontmatter
\input{frontmatter/titlepage.tex}

\BLOCK{ if frontmatter and frontmatter.strip() }
\VAR{frontmatter}
\BLOCK{ endif }

\input{frontmatter/copyrightpage.tex}

\BLOCK{ if preface_body and preface_body.strip() }
\input{frontmatter/preface.tex}
\BLOCK{ endif }

\tableofcontents
\thispagestyle{empty}
\cleardoublepage

\mainmatter
\pagestyle{fancy}
\VAR{mainmatter}

\backmatter
\BLOCK{ if backmatter and backmatter.strip() }
\VAR{backmatter}
\BLOCK{ endif }

\BLOCK{ if back_cover_text and back_cover_text.strip() }
\input{frontmatter/backpage.tex}
\BLOCK{ endif }

\BLOCK{ if caraumabibliographystyle and caraumabibliographystyle.strip() }
\bibliographystyle{\caraumabibliographystyle}
\BLOCK{ endif }

\BLOCK{ if caraumabibliography and caraumabibliography.strip() }
\bibliography{\caraumabibliography}
\BLOCK{ endif }

\end{document}
