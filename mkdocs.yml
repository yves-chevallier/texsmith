site_name: TeXSmith
site_url: https://yves-chevallier.github.io/texsmith
site_author: Yves Chevallier
repo_name: yves-chevallier/texsmith
repo_url: https://github.com/yves-chevallier/texsmith
copyright: Copyright &copy; 2025 Yves Chevallier
theme:
  language: en
  name: material
  logo: assets/ts-anvil.svg
  font:
    text: Source Sans 3
  features:
    - search
    - navigation.instant
    - navigation.tabs
    - navigation.top
    - navigation.indexes
    - navigation.tracking
    - navigation.expand
    - navigation.sections
    - navigation.footer
    - navigation.path
    - toc.follow
    - content.code.copy
    - content.code.annotate
    - content.tabs.link
    - search.highlight
    - search.share
    - search.suggest
  palette:
    - scheme: default
      primary: brown
      accent: amber
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - scheme: slate
      primary: brown
      accent: amber
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode
  icon:
    repo: fontawesome/brands/github
plugins:
  - search
  - awesome-nav
  - autorefs:
      resolve_closest: true
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          options:
            show_source: true
            docstring_section_style: list
            show_docstring_description: true
            show_signature_annotations: true
            docstring_style: google
            separate_signature: true
            merge_init_into_class: true
  - drawio:
      viewer_js: assets/drawio-viewer.js
  - texsmith:
      template: book
      build_dir: press
      books:
        - title: "TeXSmith Documentation"
          folder: texsmith-docs
          root: "__texsmith_full_navigation__"
          base_level: -1
          slots:
            preface: Home
          press:
            paragraph:
              indent: 0
              spacing: 0.5em
extra:
  version:
    # The version selector Material's bundle builds from the `versions.json`
    # of the gh-pages branch; `scripts/publish_docs.py` writes that file and
    # the `latest/` redirects `mike` used to. `mike` is the only provider the
    # bundle knows, and the key is read by MkDocs and by Zensical alike.
    provider: mike
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/yves-chevallier/texsmith
markdown_extensions:
  # The web lowering. Under MkDocs the 'texsmith' plugin does the work
  # through its hooks and the extension stands aside; under Zensical, which
  # has no plugin hooks, the extension is the whole of it.
  - texsmith.site.web
  - abbr
  - admonition
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.details
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.critic
  - pymdownx.inlinehilite
  - pymdownx.caret
  - pymdownx.keys
  - pymdownx.mark
  - pymdownx.tilde
  - pymdownx.snippets:
      check_paths: true
      # Zensical's loader does not know '!relative'; every documented
      # command runs from the repository root, so a plain path does.
      base_path: .
  - pymdownx.blocks.html
  - pymdownx.blocks.caption
  - toc:
      permalink: true
  - def_list
  - footnotes
  - attr_list
  - md_in_html
  - pymdownx.arithmatex:
      generic: true
extra_javascript:
  - js/mathjax.js
  - https://unpkg.com/mathjax@3/es5/tex-mml-chtml.js
  - https://cdn.jsdelivr.net/npm/vega@5
  - https://cdn.jsdelivr.net/npm/vega-lite@5
  - https://cdn.jsdelivr.net/npm/vega-embed@6
extra_css:
  - css/brand.css
  - css/extra.css
  # A generated file: the plugin makes it under MkDocs, 'texsmith site
  # assets' under Zensical. Zensical reads this list before Python runs, so
  # it is listed here rather than added by the extension.
  - assets/texsmith/texsmith.css
exclude_docs: |
  assets/**/*.md
