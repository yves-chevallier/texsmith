# Font management

The builtins template fragments include `ts-fonts`, which sets up a sensible font stack with Noto as the main family and various fallbacks for missing glyphs. This works out of the box for most languages and scripts.

In traditional LaTeX, with default Latin Modern fonts, the combination of styles is full:

