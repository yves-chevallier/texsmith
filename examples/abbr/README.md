# Abbreviations

This example shows how TeXSmith handles abbreviations and their automatic definitions in a short document. It exists to ensure the abbreviation extensions keep working after changes.
