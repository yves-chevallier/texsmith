# Nested lists

Six levels of bullets, of numbers, and of the two alternating, typeset with the
`book` template. It exists to keep the shared `texsmith-lists` package — the one
that raises LaTeX's four-level `itemize`/`enumerate` limit and its six-list
`Too deeply nested` error to nine levels — under the parity gate, `.tex` and PDF
alike.
