# TMark Syntax

The TMark dialect construct by construct, each shown as source and as the typeset result. Doubles as a regression sheet for rich conversions (tables, callouts, code, diagrams, raw passthrough, includes).
